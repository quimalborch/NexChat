<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NexChat.ConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NexChat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Configuración">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" 
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              Padding="20,10"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="0,0,0,1">
            <TextBlock Text="Configuración"
                       FontSize="24"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Navigation Panel -->
            <Grid Grid.Column="0" 
                  Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                  BorderThickness="0,0,1,0"
                  Padding="10">
                <StackPanel Spacing="8">
                    <Button x:Name="BtnIdentidad"
                            Click="BtnIdentidad_Click"
                            Tag="General"
                            Background="Transparent"
                            HorizontalAlignment="Stretch"
                            Padding="12,8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE713;" FontSize="16"/>
                            <TextBlock Text="General" FontSize="14"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Right Content Panel -->
            <Grid Grid.Column="1" Padding="20">
                <!-- General Content -->
                <StackPanel x:Name="IdentidadContent" Visibility="Collapsed" Spacing="20">
                    <TextBlock Text="Configuración de identidad" FontSize="20" FontWeight="Bold"/>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TextBoxIdentidad" Grid.Column="0" PlaceholderText="Cambiar nombre de usuario" TextChanged="TextBoxIdentidad_TextChanged"/>
                        <CheckBox Grid.Column="1" VerticalAlignment="Center" Visibility="Collapsed" x:Name="CheckConfirmarNewIdentidad" Click="CheckConfirmarNewIdentidad_Click">
                            <ToolTipService.ToolTip>
                                <ToolTip VerticalOffset="-50" Content="Confirmar cambio"/>
                            </ToolTipService.ToolTip>
                        </CheckBox>
                    </Grid>
                    <TextBlock x:Name="TextGuildUsuario" Text="XXXXXXXXXX"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>