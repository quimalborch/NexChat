<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NexChat.ConfigWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NexChat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Configuración">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" 
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              Height="48"
              Padding="24,0">
            <TextBlock Text="Configuración"
                       FontSize="16"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Navigation Panel -->
            <Grid Grid.Column="0" 
                  Background="{ThemeResource LayerFillColorDefaultBrush}"
                  Padding="12,16">
                <StackPanel Spacing="4">
                    <TextBlock Text="AJUSTES" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="12,0,0,8"/>
                    
                    <Button x:Name="BtnIdentidad"
                            Click="BtnIdentidad_Click"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="12,10"
                            CornerRadius="4"
                            Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                            BorderThickness="0">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon Glyph="&#xE77B;" FontSize="16"/>
                            <TextBlock Text="General" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Right Content Panel -->
            <ScrollViewer Grid.Column="1" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalAlignment="Stretch">
                <!-- General Content -->
                <StackPanel x:Name="IdentidadContent" 
                            Visibility="Collapsed" 
                            Spacing="32"
                            HorizontalAlignment="Stretch"
                            Margin="32,24">
                    
                    <!-- Header Section -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Información de usuario" 
                                   FontSize="24" 
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="Gestiona tu perfil y configuración personal" 
                                   FontSize="14"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <!-- User ID Card -->
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="20">
                        <StackPanel Spacing="12">
                            <StackPanel Spacing="4">
                                <TextBlock Text="ID de Usuario" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="Identificador único de tu cuenta" 
                                           FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                            
                            <Border Background="{ThemeResource ControlFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="12,8">
                                <TextBlock x:Name="TextGuildUsuario" 
                                           Text="XXXXXXXXXX"
                                           FontFamily="Consolas"
                                           FontSize="14"
                                           IsTextSelectionEnabled="True"/>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Username Change Card -->
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="20">
                        <StackPanel Spacing="16">
                            <StackPanel Spacing="4">
                                <TextBlock Text="Nombre de usuario" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="Modifica tu nombre visible en la aplicación" 
                                           FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                            
                            <StackPanel Spacing="12">
                                <TextBox x:Name="TextBoxIdentidad" 
                                         PlaceholderText="Introduce tu nuevo nombre de usuario"
                                         TextChanged="TextBoxIdentidad_TextChanged"
                                         MaxLength="32"/>
                                
                                <Button x:Name="BtnConfirmarNewIdentidad"
                                        Click="BtnConfirmarNewIdentidad_Click"
                                        Visibility="Collapsed"
                                        Style="{StaticResource AccentButtonStyle}"
                                        HorizontalAlignment="Right"
                                        Padding="24,8">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                                        <TextBlock Text="Guardar cambios"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>