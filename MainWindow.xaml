<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NexChat.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NexChat.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NexChat">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowSpacing="10" Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Create Chat Button -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10">
            <Button x:Name="BtnCrearNuevoChat" 
                    Content="Crear Chat" 
                    Width="120" 
                    Height="40" 
                    Click="BtnCrearNuevoChat_Click"
                    CornerRadius="8"/>
            <TextBlock Text="Mis Chats" 
                       VerticalAlignment="Center" 
                       FontSize="20" 
                       FontWeight="Bold"/>
        </StackPanel>

        <!-- Chat List - ListView with Selection -->
        <ListView x:Name="ChatListView" 
                  Grid.Row="1" 
                  ItemsSource="{x:Bind ChatItems}"
                  SelectionMode="Single"
                  ItemClick="ChatListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Chat">
                    <Grid Padding="12" Background="Transparent">
                        <StackPanel Orientation="Vertical" Spacing="4">
                            <TextBlock Text="{x:Bind Name}" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="aleix123" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
